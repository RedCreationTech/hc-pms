# HC-PMS 整体完成计划 (无外部依赖范围)

状态: 2026-09-24 起执行. 本计划回答"剩余范围怎样逐步做完", 以 [功能验收矩阵](11-feature-acceptance-matrix.md) 的逐行状态为唯一进度口径, 不给未经测算的日历承诺. 用户于 2026-09-24 确认: 待合同的外部集成行 (CRM / OA / ERP / PLM / MES / SRM / 销服物料 / BI) 本轮不做, 不以模拟冒充; 由 Claude 出整体计划后逐增量推进, 每个增量云端开发后同步回本机本地提交, 由用户推送.

## 剩余范围 (截至 2026-09-24 矩阵)

| 范围 | 行数 | 已完成 (verified / implemented-local) | 部分 | 未开始 | 说明 |
|---|---|---|---|---|---|
| PPT 对应行 A-G | 85 | 25 | 40 | 20 | 其中 31 行依赖外部接口合同 (18 行完全待合同, 13 行只做了本地子集) |
| PPT 无外部依赖行 | 54 | 25 | 27 | 2 | 27 个"部分"里 19 行卡在待批准业务口径 (待规则), 8 行是本地能力缺口; 未开始为 G17 私有部署, G18 客户端兼容 |
| 标准 PMS 补全 H01-H21 | 21 | 2 | 15 | 4 | 全部无外部依赖; 未开始为 H12 承诺成本 / 预测完工, H13 财务期间 / 多币种, H20 UAT 上线, H21 AI |

"完成"的定义沿用矩阵: 复合行不因某个子能力上行; 待规则行即使做成可配置规则 + 工程默认值, 状态仍保持 `partial / 待规则` 直到业务口径批准; 待合同行不动. 每个增量的放行标准相同: 领域 / HTTP 合同测试 + 全量 PMS 回归 0 失败 + shadow-cljs 0 warnings + 真实 Playwright 截图 + 矩阵 / 验证记录 / 合同文档同步 + 端到端报告.

## 增量顺序与内容

| 增量 | 目标行 | 交付内容 | 放行标准 | 完成后仍保持的边界 |
|---|---|---|---|---|
| 5 文档二进制附件与证据链 | C04, C05, C06, (A09 文档类别, H10 部分) | 真实文件上传 (multipart, 大小 / 类型白名单), 内容寻址存储 (SHA256 即存储键, 版本不可变), 预览 (PDF / 图片 / 文本) 与下载 (读取时复核 SHA256), 批量 ZIP 含二进制, 密级 / 阶段 / 结构节点 / 文档类别归集, 发布审批与受控签发记录 (签发人 / 时间 / 摘要, 非法定电子签章), Gate / DQ / 交底 / 收尾 / 试验证据引用二进制文档; 同增量在云端安装 MySQL 8 实际执行全部迁移与全量回归, 关闭 "MySQL 未执行" 缺口 | 新增文档测试命名空间 + 全量回归; Playwright 真实上传 PDF / 图片并下载比对 SHA256 | C06 正式电子签章 (外部 CA) 与外部文书模板仍待; 密级平台化 (BU 节点授权) 归 H17 |
| 6 计划与进度深化 | B02, B03, B12, B15, B19, H04, H06 | 单机 / 子项目计划从模板与节点自动派生并可重排 (重排走已批准变更), 阶段权重项目级覆盖 (工程默认 + 可配置), 挣值 (PV / EV / AC, SPI / CPI, 完工预测) 只读派生并入过程看板与组合看板, 交接 / SAT 关口检查项目录化, 本地定时扫描 (Quartz) 生成逾期 / 临期待办与升级建议 (只写本地待办, 不投递外部) | 规划 / 治理测试新增用例 + 全量回归; 浏览器: 派生单机计划 -> 反馈 -> EVM 面板 -> 定时扫描结果 | B02 / B19 待规则 (权重与提醒口径) 状态不上行; B12 / B15 检查项企业口径待批准 |
| 7 财务深化 | F02, F05, F06, F08, F09, H07, H12, H13 | 预算科目字典 (工程默认 + 可配置), 有效期费率表 (角色 / 人员, 版本化) 使批准工时自动生成人工成本, 承诺成本 (已批准备料 / 包材申请金额) 与预测完工成本 (EAC / ETC) 只读派生, 财务期间与封期联动 (期间关账后版本只读), 多币种汇率表 (版本化, 换算仅用于看板口径) 与净利 / 毛利对比, 季度经营目标扩展毛利 / 结项 / 准时率复算 | 财务测试新增用例 + 全量回归; 浏览器: 费率 -> 工时 -> 人工成本 -> EAC -> 期间关账 -> 目标达成 | F05 / F06 / F08 / F09 待规则 (费率 / 税额 / 决算 / 净利口径) 状态不上行; F01 / F03 / F07 待合同不动 |
| 8 待规则行配置化 | A07, A10, A11, B06, B11, C03, D03, D06, E04, E07, E08, E10, G18 | 把每条待规则行的"口径"做成平台配置 (kind: gate-mapping G1-G8 适用编号, kitting-rule 齐套分母 / 替代料, field-calendar 交底与现场自然日或工作日, coding-rule 对象收集规则, packaging-catalog 包材清单, preship-catalog 发货前条件目录, closure-catalog 收尾清单, trace-rule 覆盖率分母与偏差校准) 并附工程默认值与"待业务批准"标识; 研发类项目独立流程配置; 浏览器兼容矩阵以 Playwright 多引擎 (chromium / firefox / webkit) 实测记录 | 配置测试扩展 + 全量回归; 浏览器: 每类规则改配置后业务路径随之变化 | 全部保持 `partial / 待规则`, 直到业务代表批准口径后才可标 implemented |
| 9 标准 PMS 补全 | H01, H03, H05, H08, H09, H10, H11, H14, H15, H16, H17, H18 | 章程业务目标与收益跟踪, 范围基线 WBS 字典与排除项, 技能字典 / 请假 / 分派冲突, 风险问题复审策略与批量重开, 变更控制委员会 (CCB) 决策记录与影响矩阵, 质量计划 / 验收准则 / 检验记录, 正式交付签收与遗留项承诺, 收尾归档快照与只读检索, 经验教训回写模板建议, 成员撤销的历史访问规则, 节点级字段密级与外部参与者角色, 资料保留策略与归档 | 各模块测试扩展 + 全量回归; 浏览器: 每行至少一条端到端 | H12 / H13 在增量 7; H19 / H20 在增量 10; H21 AI 需模型接入 (外部依赖) 不在本计划 |
| 10 生产就绪 | G17, H19, H20 (脱敏 UAT 剧本), G18 | Dockerfile / docker-compose (应用 + MySQL 8) / K8S 清单与私有部署说明, 备份恢复与升级回退 (migratus down / up) 演练脚本及记录, 性能基线 (本地压测工具对组合看板 / 待办 / 检索), 日志字段与健康检查, 脱敏演示数据集与 UAT 剧本 (按完整订单闭环), 浏览器兼容矩阵 | 隔离环境实际演练记录 (不是脚本存在即通过); MySQL 全量回归常态化 | 真实生产目标环境 / 容量 / 业务 UAT 签收仍须用户环境完成; G17 / G18 视演练结果决定是否上行 |

增量 5 立即开始; 6-10 按序推进, 每个增量结束时更新本表 (完成日期, 实际测试数字, 矩阵行变化), 若某增量暴露前置问题 (如 MySQL 迁移差异) 优先修复再继续.

### 进度记录

| 增量 | 完成日期 | 实际结果 | 矩阵行变化 |
|---|---|---|---|
| 5 文档二进制附件与证据链 | 2026-09-24 | 文档附件测试 5/68; 全量回归 SQLite 128/1272, MySQL 8.0.46 128/1241 (云端真实执行, 关闭 "MySQL 未执行" 缺口); 前端 0 warnings; `pms-d-documents.spec.js` 1 passed, 既有文档用例复跑通过 (c05 仅环境文件名断言) | C04 / C05 / C06 partial -> implemented / local; A09 文档类别进入文档归集 |
| 6 计划与进度深化 | 2026-09-24 | 计划与进度深化测试 8/112; 全量回归 SQLite 136/1384, MySQL 8.0.46 136/1353 (首轮暴露共享库下模板版本依赖, 测试改为按目录恢复阶段定义后通过); 迁移 202609240002 双库 down/up 探针通过 (SQLite down 补 DROP COLUMN); 前端 0 warnings; `pms-e-progress.spec.js` 1 passed (含定时任务 9001 "执行一次" 经 Quartz 真实调用扫描); 全量 PMS 浏览器套件 52 passed / 1 failed (仅 c05 环境文件名断言) | B03 partial -> implemented / local; H04 补节点重排证据; B02 / B19 保持 partial / 待规则 (权重与提醒口径), B12 / B15 保持 partial (检查项企业口径), H06 保持 partial (反馈独立审核与偏差措施仍待) |

## 不在本计划内 (如实列出)

- 八套外部系统适配器与依赖它们的行 (A04-A06, B04 同步, B14 CRM 推送, B17, B18 解冻, C11 外部通知, D01 / D02 回写, D04 / D05 / D07, E01-E03 MES 回传, E05 清单, E06, E09 回传, F01 / F03 / F07 权威账项, G07-G15): 需要接口字段 / 认证 / 沙箱 / 责任人后另立计划.
- H21 AI 问答与建议: 需要模型接入与评估, 属外部依赖.
- 待规则行的最终验收: 需业务代表批准口径 (Gate 编号映射, 编码规则, 交底日历, 齐套分母, 财务口径), 增量 8 只把它们做成可配置并给工程默认值.
- 生产环境本身: 增量 10 提供部署件与演练记录, 真实目标环境的升级 / 恢复 / 容量 / UAT 签收在用户环境完成.
